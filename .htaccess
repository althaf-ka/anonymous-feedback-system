# Root .htaccess (at project root)
RewriteEngine On

# Send all requests to the public folder unless the file exists
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/index.php [L]
